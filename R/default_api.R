#' FastAPI
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: 0.1.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Default operations
#' @description DefaultApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AddCollaboratorPut  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_account_id <- "account_id_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_role <- "read" # character |  (Optional)
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Add Collaborator
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  CreateInstanceInstancesPut  ####################
#'
#' library(laminr.api)
#' var_name <- "name_example" # character |
#' var_storage <- "create-s3" # character |  (Optional)
#' var_schema_str <- "schema_str_example" # character |  (Optional)
#' var_db_server_key <- "db_server_key_example" # character |  (Optional)
#' var_db_server_url <- "db_server_url_example" # character |  (Optional)
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Create Instance
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  CreateRecordPut  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_body <- TODO # AnyType |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Create Record
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  DeleteCollaboratorDelete  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_account_id <- "account_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Delete Collaborator
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  DeleteInstanceDelete  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_instance_name <- "instance_name_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Delete Instance
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  DeleteRecordDelete  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_uid <- "uid_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Delete Record
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GeneratePasswordGet  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Generate Password
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GenerateUrlGet  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Generate Url
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GetInstanceStatisticsGet  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_q <- c("inner_example") # array[character] | In module + "." + model format (case-sensitive) (Optional)
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Get Instance Statistics
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GetNonEmptyTablesGet  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Get Non Empty Tables
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GetRecordPost  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_id_or_uid <- "id_or_uid_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_limit_to_many <- 10 # integer |  (Optional)
#' var_include_foreign_keys <- FALSE # character |  (Optional)
#' var_authorization <- "authorization_example" # character |  (Optional)
#' var_get_record_request_body <- GetRecordRequestBody$new(c("select_example")) # GetRecordRequestBody |  (Optional)
#'
#' # Get Record
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GetRecordsPost  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_limit <- 50 # integer |  (Optional)
#' var_offset <- 0 # integer |  (Optional)
#' var_limit_to_many <- 10 # integer |  (Optional)
#' var_include_foreign_keys <- FALSE # character |  (Optional)
#' var_authorization <- "authorization_example" # character |  (Optional)
#' var_get_records_request_body <- GetRecordsRequestBody$new(c("select_example"), 123, c(OrderByColumn$new("field_example", "descending_example")), "search_example") # GetRecordsRequestBody |  (Optional)
#'
#' # Get Records
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GetRelationCountsGet  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_id <- 56 # integer |
#' var_schema_id <- "schema_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Get Relation Counts
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GetRelationsGet  ####################
#'
#' library(laminr.api)
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_instance_id <- "instance_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Get Relations
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GetSchemaGet  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Get Schema
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GetValuesPost  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_field_path <- "field_path_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_limit <- 50 # integer |  (Optional)
#' var_offset <- 0 # integer |  (Optional)
#' var_authorization <- "authorization_example" # character |  (Optional)
#' var_get_values_request_body <- GetValuesRequestBody$new(123, c(OrderByColumn$new("field_example", "descending_example")), "search_example") # GetValuesRequestBody |  (Optional)
#'
#' # Get Values
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GrantS3PermissionsPut  ####################
#'
#' library(laminr.api)
#' var_bucket_name <- "bucket_name_example" # character |
#' var_s3_permissions_request <- S3PermissionsRequest$new("aws_access_key_id_example", "aws_secret_access_key_example", "region_example") # S3PermissionsRequest |
#' var_aws_account_id <- "767398070972" # character |  (Optional)
#' var_aws_user_name <- "lamin-manager" # character |  (Optional)
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Grant S3 Permissions
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  GroupByPost  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_group_by_request_body <- GroupByRequestBody$new(c(Dimension$new("field_name_example", "count")), c(Measure$new("field_name_example", "count", "alias_example")), 123) # GroupByRequestBody |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Group By
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  TransferOwnershipPatch  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_handle <- "handle_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Transfer Ownership
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  UpdateCollaboratorPatch  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_account_id <- "account_id_example" # character |
#' var_role <- "role_example" # character |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Update Collaborator
#' api_instance <- DefaultApi$new()
#'
#'
#'
#' ####################  UpdateRecordPatch  ####################
#'
#' library(laminr.api)
#' var_instance_id <- "instance_id_example" # character |
#' var_module_name <- "module_name_example" # character |
#' var_model_name <- "model_name_example" # character |
#' var_uid <- "uid_example" # character |
#' var_schema_id <- "schema_id_example" # character |
#' var_body <- TODO # AnyType |
#' var_authorization <- "authorization_example" # character |  (Optional)
#'
#' # Update Record
#' api_instance <- DefaultApi$new()
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @importFrom rlang abort
#' @export
DefaultApi <- R6::R6Class(
  "DefaultApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new DefaultApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Add Collaborator
    #'
    #' @param instance_id
    #' @param account_id
    #' @param schema_id
    #' @param role (optional) No description (default value: "read")
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    AddCollaboratorPut = function(instance_id, account_id, schema_id, role = "read", authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$AddCollaboratorPutWithHttpInfo(instance_id, account_id, schema_id, role, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Add Collaborator
    #'
    #' @param instance_id
    #' @param account_id
    #' @param schema_id
    #' @param role (optional) No description (default value: "read")
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    AddCollaboratorPutWithHttpInfo = function(instance_id, account_id, schema_id, role = "read", authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`account_id`)) {
        rlang::abort(
          message = "Missing required parameter `account_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `account_id`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }






      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      if (!is.null(`role`) && !(`role` %in% c("admin", "write", "read"))) {
        rlang::abort(
          message = "Invalid value for `role` when calling DefaultApi$AddCollaboratorPut. Must be [admin, write, read].",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Invalid value for `role` when calling DefaultApi$AddCollaboratorPut. Must be [admin, write, read]."
          )
        )
      }
      query_params[["role"]] <- `role`

      local_var_url_path <- "/instances/{instance_id}/collaborators/{account_id}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`account_id`)) {
        local_var_url_path <- gsub("\\{account_id\\}", URLencode(as.character(`account_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "PUT",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Create Instance
    #'
    #' @param name
    #' @param storage (optional) No description (default value: "create-s3")
    #' @param schema_str (optional) No description
    #' @param db_server_key (optional) No description
    #' @param db_server_url (optional) No description
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    CreateInstanceInstancesPut = function(name, storage = "create-s3", schema_str = NULL, db_server_key = NULL, db_server_url = NULL, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateInstanceInstancesPutWithHttpInfo(name, storage, schema_str, db_server_key, db_server_url, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Create Instance
    #'
    #' @param name
    #' @param storage (optional) No description (default value: "create-s3")
    #' @param schema_str (optional) No description
    #' @param db_server_key (optional) No description
    #' @param db_server_url (optional) No description
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    CreateInstanceInstancesPutWithHttpInfo = function(name, storage = "create-s3", schema_str = NULL, db_server_key = NULL, db_server_url = NULL, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        rlang::abort(
          message = "Missing required parameter `name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `name`."
          )
        )
      }







      header_params["Authorization"] <- `authorization`

      query_params[["name"]] <- `name`

      query_params[["storage"]] <- `storage`

      query_params[["schema_str"]] <- `schema_str`

      query_params[["db_server_key"]] <- `db_server_key`

      query_params[["db_server_url"]] <- `db_server_url`

      local_var_url_path <- "/instances"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "PUT",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Create Record
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param schema_id
    #' @param body
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    CreateRecordPut = function(instance_id, module_name, model_name, schema_id, body, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateRecordPutWithHttpInfo(instance_id, module_name, model_name, schema_id, body, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Create Record
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param schema_id
    #' @param body
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    CreateRecordPutWithHttpInfo = function(instance_id, module_name, model_name, schema_id, body, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }

      if (missing(`body`)) {
        rlang::abort(
          message = "Missing required parameter `body`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `body`."
          )
        )
      }







      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/instances/{instance_id}/modules/{module_name}/{model_name}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "PUT",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Delete Collaborator
    #'
    #' @param instance_id
    #' @param account_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    DeleteCollaboratorDelete = function(instance_id, account_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$DeleteCollaboratorDeleteWithHttpInfo(instance_id, account_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Delete Collaborator
    #'
    #' @param instance_id
    #' @param account_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    DeleteCollaboratorDeleteWithHttpInfo = function(instance_id, account_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`account_id`)) {
        rlang::abort(
          message = "Missing required parameter `account_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `account_id`."
          )
        )
      }




      header_params["Authorization"] <- `authorization`

      local_var_url_path <- "/instances/{instance_id}/collaborators/{account_id}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`account_id`)) {
        local_var_url_path <- gsub("\\{account_id\\}", URLencode(as.character(`account_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "DELETE",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Delete Instance
    #'
    #' @param instance_id
    #' @param instance_name
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    DeleteInstanceDelete = function(instance_id, instance_name, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$DeleteInstanceDeleteWithHttpInfo(instance_id, instance_name, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Delete Instance
    #'
    #' @param instance_id
    #' @param instance_name
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    DeleteInstanceDeleteWithHttpInfo = function(instance_id, instance_name, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`instance_name`)) {
        rlang::abort(
          message = "Missing required parameter `instance_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_name`."
          )
        )
      }




      header_params["Authorization"] <- `authorization`

      query_params[["instance_name"]] <- `instance_name`

      local_var_url_path <- "/instances/{instance_id}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "DELETE",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Delete Record
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param uid
    #' @param schema_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    DeleteRecordDelete = function(instance_id, module_name, model_name, uid, schema_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$DeleteRecordDeleteWithHttpInfo(instance_id, module_name, model_name, uid, schema_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Delete Record
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param uid
    #' @param schema_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    DeleteRecordDeleteWithHttpInfo = function(instance_id, module_name, model_name, uid, schema_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`uid`)) {
        rlang::abort(
          message = "Missing required parameter `uid`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `uid`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }







      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      local_var_url_path <- "/instances/{instance_id}/modules/{module_name}/{model_name}/{uid}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`uid`)) {
        local_var_url_path <- gsub("\\{uid\\}", URLencode(as.character(`uid`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "DELETE",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Generate Password
    #'
    #' @param instance_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GeneratePasswordGet = function(instance_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GeneratePasswordGetWithHttpInfo(instance_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Generate Password
    #'
    #' @param instance_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GeneratePasswordGetWithHttpInfo = function(instance_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }



      header_params["Authorization"] <- `authorization`

      local_var_url_path <- "/instances/{instance_id}/db/password"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "GET",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Generate Url
    #'
    #' @param instance_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GenerateUrlGet = function(instance_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GenerateUrlGetWithHttpInfo(instance_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Generate Url
    #'
    #' @param instance_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GenerateUrlGetWithHttpInfo = function(instance_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }



      header_params["Authorization"] <- `authorization`

      local_var_url_path <- "/instances/{instance_id}/db/url"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "GET",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get Instance Statistics
    #'
    #' @param instance_id
    #' @param schema_id
    #' @param q (optional) In module + "." + model format (case-sensitive)
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GetInstanceStatisticsGet = function(instance_id, schema_id, q = NULL, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetInstanceStatisticsGetWithHttpInfo(instance_id, schema_id, q, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get Instance Statistics
    #'
    #' @param instance_id
    #' @param schema_id
    #' @param q (optional) In module + "." + model format (case-sensitive)
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GetInstanceStatisticsGetWithHttpInfo = function(instance_id, schema_id, q = NULL, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }





      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      # explore
      for (query_item in `q`) {
        query_params[["q"]] <- c(query_params[["q"]], list(`q` = query_item))
      }

      local_var_url_path <- "/instances/{instance_id}/statistics"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "GET",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get Non Empty Tables
    #'
    #' @param instance_id
    #' @param schema_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GetNonEmptyTablesGet = function(instance_id, schema_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetNonEmptyTablesGetWithHttpInfo(instance_id, schema_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get Non Empty Tables
    #'
    #' @param instance_id
    #' @param schema_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GetNonEmptyTablesGetWithHttpInfo = function(instance_id, schema_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }




      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      local_var_url_path <- "/instances/{instance_id}/non_empty_tables"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "GET",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get Record
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param id_or_uid
    #' @param schema_id
    #' @param limit_to_many (optional) No description (default value: 10)
    #' @param include_foreign_keys (optional) No description (default value: FALSE)
    #' @param authorization (optional) No description
    #' @param get_record_request_body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GetRecordPost = function(instance_id, module_name, model_name, id_or_uid, schema_id, limit_to_many = 10, include_foreign_keys = FALSE, authorization = NULL, get_record_request_body = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRecordPostWithHttpInfo(instance_id, module_name, model_name, id_or_uid, schema_id, limit_to_many, include_foreign_keys, authorization, get_record_request_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get Record
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param id_or_uid
    #' @param schema_id
    #' @param limit_to_many (optional) No description (default value: 10)
    #' @param include_foreign_keys (optional) No description (default value: FALSE)
    #' @param authorization (optional) No description
    #' @param get_record_request_body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GetRecordPostWithHttpInfo = function(instance_id, module_name, model_name, id_or_uid, schema_id, limit_to_many = 10, include_foreign_keys = FALSE, authorization = NULL, get_record_request_body = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`id_or_uid`)) {
        rlang::abort(
          message = "Missing required parameter `id_or_uid`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `id_or_uid`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }










      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      query_params[["limit_to_many"]] <- `limit_to_many`

      query_params[["include_foreign_keys"]] <- `include_foreign_keys`

      if (!is.null(`get_record_request_body`)) {
        local_var_body <- `get_record_request_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/instances/{instance_id}/modules/{module_name}/{model_name}/{id_or_uid}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`id_or_uid`)) {
        local_var_url_path <- gsub("\\{id_or_uid\\}", URLencode(as.character(`id_or_uid`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "POST",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get Records
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param schema_id
    #' @param limit (optional) No description (default value: 50)
    #' @param offset (optional) No description (default value: 0)
    #' @param limit_to_many (optional) No description (default value: 10)
    #' @param include_foreign_keys (optional) No description (default value: FALSE)
    #' @param authorization (optional) No description
    #' @param get_records_request_body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GetRecordsPost = function(instance_id, module_name, model_name, schema_id, limit = 50, offset = 0, limit_to_many = 10, include_foreign_keys = FALSE, authorization = NULL, get_records_request_body = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRecordsPostWithHttpInfo(instance_id, module_name, model_name, schema_id, limit, offset, limit_to_many, include_foreign_keys, authorization, get_records_request_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get Records
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param schema_id
    #' @param limit (optional) No description (default value: 50)
    #' @param offset (optional) No description (default value: 0)
    #' @param limit_to_many (optional) No description (default value: 10)
    #' @param include_foreign_keys (optional) No description (default value: FALSE)
    #' @param authorization (optional) No description
    #' @param get_records_request_body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GetRecordsPostWithHttpInfo = function(instance_id, module_name, model_name, schema_id, limit = 50, offset = 0, limit_to_many = 10, include_foreign_keys = FALSE, authorization = NULL, get_records_request_body = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }











      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      query_params[["limit_to_many"]] <- `limit_to_many`

      query_params[["include_foreign_keys"]] <- `include_foreign_keys`

      if (!is.null(`get_records_request_body`)) {
        local_var_body <- `get_records_request_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/instances/{instance_id}/modules/{module_name}/{model_name}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "POST",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get Relation Counts
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param id
    #' @param schema_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GetRelationCountsGet = function(instance_id, module_name, model_name, id, schema_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRelationCountsGetWithHttpInfo(instance_id, module_name, model_name, id, schema_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get Relation Counts
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param id
    #' @param schema_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GetRelationCountsGetWithHttpInfo = function(instance_id, module_name, model_name, id, schema_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`id`)) {
        rlang::abort(
          message = "Missing required parameter `id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `id`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }







      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      local_var_url_path <- "/instances/{instance_id}/modules/{module_name}/{model_name}/{id}/counts"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "GET",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get Relations
    #'
    #' @param module_name
    #' @param model_name
    #' @param instance_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GetRelationsGet = function(module_name, model_name, instance_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRelationsGetWithHttpInfo(module_name, model_name, instance_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get Relations
    #'
    #' @param module_name
    #' @param model_name
    #' @param instance_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GetRelationsGetWithHttpInfo = function(module_name, model_name, instance_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }





      header_params["Authorization"] <- `authorization`

      local_var_url_path <- "/instances/{instance_id}/schema/{module_name}/{model_name}"
      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "GET",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get Schema
    #'
    #' @param instance_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GetSchemaGet = function(instance_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetSchemaGetWithHttpInfo(instance_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get Schema
    #'
    #' @param instance_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GetSchemaGetWithHttpInfo = function(instance_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }



      header_params["Authorization"] <- `authorization`

      local_var_url_path <- "/instances/{instance_id}/schema"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "GET",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get Values
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param field_path
    #' @param schema_id
    #' @param limit (optional) No description (default value: 50)
    #' @param offset (optional) No description (default value: 0)
    #' @param authorization (optional) No description
    #' @param get_values_request_body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GetValuesPost = function(instance_id, module_name, model_name, field_path, schema_id, limit = 50, offset = 0, authorization = NULL, get_values_request_body = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetValuesPostWithHttpInfo(instance_id, module_name, model_name, field_path, schema_id, limit, offset, authorization, get_values_request_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get Values
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param field_path
    #' @param schema_id
    #' @param limit (optional) No description (default value: 50)
    #' @param offset (optional) No description (default value: 0)
    #' @param authorization (optional) No description
    #' @param get_values_request_body (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GetValuesPostWithHttpInfo = function(instance_id, module_name, model_name, field_path, schema_id, limit = 50, offset = 0, authorization = NULL, get_values_request_body = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`field_path`)) {
        rlang::abort(
          message = "Missing required parameter `field_path`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `field_path`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }










      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      if (!is.null(`get_values_request_body`)) {
        local_var_body <- `get_values_request_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/instances/{instance_id}/modules/{module_name}/{model_name}/fields/{field_path}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`field_path`)) {
        local_var_url_path <- gsub("\\{field_path\\}", URLencode(as.character(`field_path`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "POST",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Grant S3 Permissions
    #'
    #' @param bucket_name
    #' @param s3_permissions_request
    #' @param aws_account_id (optional) No description (default value: "767398070972")
    #' @param aws_user_name (optional) No description (default value: "lamin-manager")
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GrantS3PermissionsPut = function(bucket_name, s3_permissions_request, aws_account_id = "767398070972", aws_user_name = "lamin-manager", authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GrantS3PermissionsPutWithHttpInfo(bucket_name, s3_permissions_request, aws_account_id, aws_user_name, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Grant S3 Permissions
    #'
    #' @param bucket_name
    #' @param s3_permissions_request
    #' @param aws_account_id (optional) No description (default value: "767398070972")
    #' @param aws_user_name (optional) No description (default value: "lamin-manager")
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GrantS3PermissionsPutWithHttpInfo = function(bucket_name, s3_permissions_request, aws_account_id = "767398070972", aws_user_name = "lamin-manager", authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`bucket_name`)) {
        rlang::abort(
          message = "Missing required parameter `bucket_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `bucket_name`."
          )
        )
      }

      if (missing(`s3_permissions_request`)) {
        rlang::abort(
          message = "Missing required parameter `s3_permissions_request`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `s3_permissions_request`."
          )
        )
      }






      header_params["Authorization"] <- `authorization`

      query_params[["aws_account_id"]] <- `aws_account_id`

      query_params[["aws_user_name"]] <- `aws_user_name`

      if (!is.null(`s3_permissions_request`)) {
        local_var_body <- `s3_permissions_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/storages/s3/{bucket_name}/permissions"
      if (!missing(`bucket_name`)) {
        local_var_url_path <- gsub("\\{bucket_name\\}", URLencode(as.character(`bucket_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "PUT",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Group By
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param schema_id
    #' @param group_by_request_body
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    GroupByPost = function(instance_id, module_name, model_name, schema_id, group_by_request_body, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GroupByPostWithHttpInfo(instance_id, module_name, model_name, schema_id, group_by_request_body, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Group By
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param schema_id
    #' @param group_by_request_body
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    GroupByPostWithHttpInfo = function(instance_id, module_name, model_name, schema_id, group_by_request_body, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }

      if (missing(`group_by_request_body`)) {
        rlang::abort(
          message = "Missing required parameter `group_by_request_body`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `group_by_request_body`."
          )
        )
      }







      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      if (!is.null(`group_by_request_body`)) {
        local_var_body <- `group_by_request_body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/instances/{instance_id}/modules/{module_name}/{model_name}/group-by"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "POST",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Transfer Ownership
    #'
    #' @param instance_id
    #' @param handle
    #' @param schema_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    TransferOwnershipPatch = function(instance_id, handle, schema_id, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$TransferOwnershipPatchWithHttpInfo(instance_id, handle, schema_id, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Transfer Ownership
    #'
    #' @param instance_id
    #' @param handle
    #' @param schema_id
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    TransferOwnershipPatchWithHttpInfo = function(instance_id, handle, schema_id, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`handle`)) {
        rlang::abort(
          message = "Missing required parameter `handle`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `handle`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }





      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      local_var_url_path <- "/instances/{instance_id}/owner/{handle}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`handle`)) {
        local_var_url_path <- gsub("\\{handle\\}", URLencode(as.character(`handle`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "PATCH",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Update Collaborator
    #'
    #' @param instance_id
    #' @param account_id
    #' @param role
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    UpdateCollaboratorPatch = function(instance_id, account_id, role, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$UpdateCollaboratorPatchWithHttpInfo(instance_id, account_id, role, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Update Collaborator
    #'
    #' @param instance_id
    #' @param account_id
    #' @param role
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    UpdateCollaboratorPatchWithHttpInfo = function(instance_id, account_id, role, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`account_id`)) {
        rlang::abort(
          message = "Missing required parameter `account_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `account_id`."
          )
        )
      }

      if (missing(`role`)) {
        rlang::abort(
          message = "Missing required parameter `role`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `role`."
          )
        )
      }





      header_params["Authorization"] <- `authorization`

      if (!is.null(`role`) && !(`role` %in% c("admin", "write", "read"))) {
        rlang::abort(
          message = "Invalid value for `role` when calling DefaultApi$UpdateCollaboratorPatch. Must be [admin, write, read].",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Invalid value for `role` when calling DefaultApi$UpdateCollaboratorPatch. Must be [admin, write, read]."
          )
        )
      }
      query_params[["role"]] <- `role`

      local_var_url_path <- "/instances/{instance_id}/collaborators/{account_id}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`account_id`)) {
        local_var_url_path <- gsub("\\{account_id\\}", URLencode(as.character(`account_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "PATCH",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Update Record
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param uid
    #' @param schema_id
    #' @param body
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return AnyType
    UpdateRecordPatch = function(instance_id, module_name, model_name, uid, schema_id, body, authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$UpdateRecordPatchWithHttpInfo(instance_id, module_name, model_name, uid, schema_id, body, authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Update Record
    #'
    #' @param instance_id
    #' @param module_name
    #' @param model_name
    #' @param uid
    #' @param schema_id
    #' @param body
    #' @param authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    UpdateRecordPatchWithHttpInfo = function(instance_id, module_name, model_name, uid, schema_id, body, authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`instance_id`)) {
        rlang::abort(
          message = "Missing required parameter `instance_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `instance_id`."
          )
        )
      }

      if (missing(`module_name`)) {
        rlang::abort(
          message = "Missing required parameter `module_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `module_name`."
          )
        )
      }

      if (missing(`model_name`)) {
        rlang::abort(
          message = "Missing required parameter `model_name`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `model_name`."
          )
        )
      }

      if (missing(`uid`)) {
        rlang::abort(
          message = "Missing required parameter `uid`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `uid`."
          )
        )
      }

      if (missing(`schema_id`)) {
        rlang::abort(
          message = "Missing required parameter `schema_id`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `schema_id`."
          )
        )
      }

      if (missing(`body`)) {
        rlang::abort(
          message = "Missing required parameter `body`.",
          .subclass = "ApiException",
          ApiException = ApiException$new(
            status = 0,
            reason = "Missing required parameter `body`."
          )
        )
      }








      header_params["Authorization"] <- `authorization`

      query_params[["schema_id"]] <- `schema_id`

      if (!is.null(`body`)) {
        local_var_body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/instances/{instance_id}/modules/{module_name}/{model_name}/{uid}"
      if (!missing(`instance_id`)) {
        local_var_url_path <- gsub("\\{instance_id\\}", URLencode(as.character(`instance_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`module_name`)) {
        local_var_url_path <- gsub("\\{module_name\\}", URLencode(as.character(`module_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`model_name`)) {
        local_var_url_path <- gsub("\\{model_name\\}", URLencode(as.character(`model_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`uid`)) {
        local_var_url_path <- gsub("\\{uid\\}", URLencode(as.character(`uid`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(
        url = paste0(self$api_client$base_path, local_var_url_path),
        method = "PATCH",
        query_params = query_params,
        header_params = header_params,
        form_params = form_params,
        file_params = file_params,
        accepts = local_var_accepts,
        content_types = local_var_content_types,
        body = local_var_body,
        is_oauth = is_oauth,
        oauth_scopes = oauth_scopes,
        ...
      )

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("laminr.api")),
          error = function(e) {
            rlang::abort(
              message = "Failed to deserialize response",
              .subclass = "ApiException",
              ApiException = ApiException$new(http_response = local_var_resp)
            )
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
